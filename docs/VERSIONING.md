# 版本与回退规范

Project Atlas 使用带备注的 Git 标签标记可回退里程碑。

## 标签规则

- 标签采用 `v主版本.次版本.修订版本` 格式。
- 每个完成版本使用 annotated tag，不使用无备注的 lightweight tag。
- 标签备注必须包含关联 Task、完成范围、测试结果和明确排除项。
- 提交与标签必须同时推送到 GitHub 和群晖，并分别核对目标提交。
- 已发布标签不可移动或复用；需要修正时创建新版本。

## 安全回退

查看某个版本而不改动当前分支：

```bash
git switch --detach v0.2.0
```

从旧版本创建独立恢复分支：

```bash
git switch -c restore/v0.2.0 v0.2.0
```

恢复分支不会覆盖当前 `main`。除非用户明确要求并确认影响，不使用破坏性的强制回退。

## 已建立版本

- `v0.1.0`：TASK-001，工程初始化与双远端发布基线。
- `v0.2.0`：TASK-002，核心领域模型基础。
- `v0.3.0`：TASK-003，Repository Asset 模型基础。
- `v0.3.1`：建立 Project Atlas Execution Plan Version 1.0。
- `v0.4.0`：TASK-004，显式边界的本地项目发现引擎。
- `v0.5.0`：TASK-005，有边界的项目结构分析器。
- `v0.6.0`：TASK-006，稳定本地项目身份与版本化元数据指纹。
- `v0.7.0`：TASK-007，Snapshot、Change 与 Event 项目历史领域契约。
- `v0.8.0`：TASK-008，确定性的项目结构新增、删除与修改检测。
- `v0.9.0`：TASK-009，引用完整且确定性排序的内存项目时间线。
- `v0.10.0`：TASK-010，显式、确定性且引用完整的跨项目关系图。
- `v0.11.0`：TASK-011，版本化知识记录与事务化 SQLite 本地存储。
- `v0.12.0`：TASK-012，结构化过滤与确定性本地关键词查询。
- `v0.13.0`：TASK-013，有来源、字段脱敏和字符上限的本地 AI 上下文。
- `v0.14.0`：TASK-014，可替换 Provider 与严格结构化项目理解。
- `v0.15.0`：TASK-015，显式单轮、可溯源且只读的 AI Project Assistant。
- `v0.16.0`：TASK-016，独立构建、无云资源且只读的本地 Dashboard。
- `v0.17.0`：TASK-017，支持安全区域、触控导航和本地安装的移动 Web 访问。
- `v0.18.0`：TASK-018，显式注册、确认变更且结果可审计的进程内 Command Center。
- `v0.19.0`：TASK-019，基于显式项目理解和关系图的确定性多项目智能聚合。
