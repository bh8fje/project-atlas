# Project Atlas 协作规范

本文件适用于仓库根目录及所有子目录。

## 项目原则

1. 本地优先：默认在用户设备上处理和保存项目数据。
2. 用户可控：任何扫描、分析、持久化或外部通信都必须透明且可配置。
3. 可验证：行为变更必须有测试或明确的验证记录。
4. 小步演进：一次 Task 只解决一个明确目标，不提前实现后续能力。
5. 文档同步：架构、状态或使用方式变化时同步更新对应文档。
6. 中文优先：面向用户的文案先写成专业、清晰、准确的中文，避免难懂术语和过度口语化，再按中文含义翻译其他语言；具体规范见 `docs/PRODUCT_LANGUAGE.md`。
7. 信息真实：用户界面必须标明数据来源与时效性，不得将静态示意、发布记录或占位数据表述为实时分析结果；具体原则见 `docs/DASHBOARD_DESIGN.md`。
8. 本机边界：项目目录必须由用户通过系统选择器明确批准；本机服务只监听回环地址，路径和检查结果不得传输到外部网络，移除登记不得修改用户文件。

## 开发规则

- 生产代码位于 `src/project_atlas/`，测试位于 `tests/`。
- 使用 Python 3.11+，优先使用标准库；新增依赖必须说明必要性。
- 不提交密钥、令牌、用户数据、虚拟环境、构建产物或缓存。
- 公共接口应有类型标注和简洁文档；复杂决策记录在 `docs/`。
- 不在未授权的 Task 中引入数据库、网络服务、AI 调用或界面。
- 不修改与当前 Task 无关的代码；发现额外问题时记录而非顺手扩展范围。

## Task 执行规则

每个 Task 按以下顺序执行：

1. 阅读 Task 的 Goal、Allowed Scope、Forbidden Scope 和 Acceptance Criteria。
2. 检查工作区状态与现有约束，确认没有覆盖未提交工作。
3. 只实现满足验收标准所需的最小变更。
4. 运行与改动相称的测试和检查。
5. 更新 `PROJECT_STATUS.md`，记录完成项、验证方式和待办。
6. 检查 diff，确认没有越界功能或敏感信息。
7. 创建聚焦且可描述的 Git commit。
8. 完成里程碑版本时创建带备注的 Git 标签，记录 Task、范围、测试结果和明确排除项。
9. 发布提交与版本标签时执行双远端推送：GitHub 与群晖必须同步成功；任何一端失败都不得报告发布完成。
10. 输出完成报告并停止；不得自动开始下一个 Task。

若需求与仓库规范冲突，以用户当前明确指令为准，并在交付报告中说明取舍。

## 完成定义

Task 只有在以下条件全部满足时才算完成：范围内产物齐全、测试通过、文档同步、无意外改动、Git 工作目录干净，提交信息能准确描述变更；形成新版本时已有带备注标签，且 GitHub 与群晖远端均已同步提交和标签。
